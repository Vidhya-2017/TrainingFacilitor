import { connect } from 'react-redux';
import BatchMaster from '../component/BatchMaster';

const mapDispatchToProps = {
};

const mapStateToProps = state => (
  {
  }
);

export default connect(mapStateToProps, mapDispatchToProps)(BatchMaster);
 